FROM node:20-alpine

WORKDIR /app

COPY backend/package*.json ./
RUN npm ci --only=production

COPY backend/ .

# Install PM2 globally to run the production engine daemon
RUN npm install pm2 -g

EXPOSE 5000

CMD ["pm2-runtime", "pm2.config.js"]